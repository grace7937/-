const arr = [3, 4, 5, 5, 6, 2, 2, 1.0];

const returnResult = function (array) {
  const toString = array.join('');
  
    let sameWords = toString.match(/[1-9]/);

  return 
};
console.log(returnResult(arr));
